# kaochang-controller-android11 协作提示词

## 仓库定位

- 本仓库是安卓11现场上位机 app 的独立维护仓库。
- 从 `1.3.01` 起，本仓库作为老稳定版本维护。
- 除非用户明确要求，不从 `D:\LeYe\cl-sausage-controller` 同步分支逻辑，也不要默认把新主仓库行为带入本仓库。
- 本仓库不包含下位机代码；涉及下位机能力、寄存器协议、设备动作时，必须先区分上位机和下位机责任边界。

## 最高原则

- 默认以稳定、可回退、少扩散为最高原则。
- 先阅读相关代码、日志、配置，再判断和修改；不要在未核对实现前直接假设。
- 只处理当前需求强相关问题，不主动扩大改动范围。
- 发现无关问题可以在结果里指出，但不擅自修复。
- 允许顺手处理和当前任务直接相关的小问题，例如乱码、日志口径、字段含义不清、版本或打包说明不一致。

## 修改边界

- 修改前先确认当前工作区状态；如果存在用户已有未提交改动，先说明并讨论影响。
- 不要回滚用户改动，除非用户明确要求。
- 不引入高风险依赖或大范围重构，除非用户明确要求并确认影响。
- 对老稳定版本的代码，优先采用最小改动、清晰日志、可配置开关和可回退方案。

## 现场设备行为

- 凡是可能改变现场设备行为的改动，都必须先说明影响范围、回退方案和验证方式。
- 没有明确要求时，不主动改变控制流程、寄存器含义、状态机、接口协议和默认开关。
- 涉及出餐、烤肠机控制、串口、Modbus、相机识别、MQTT、定时任务、营业状态、自清洁等链路时，要保守处理。
- 首次出现的状态字段、状态值、开关值要写清中文含义，避免口径漂移。

## 版本与打包

- 改版本时要同步说明 `versionName` 和 `versionCode` 的变化。
- 蒲公英上传必须使用 APK 文件，不要上传 AAB 文件。
- Android Studio 里生成蒲公英包时选择 `APK`，不要选择 `Android App Bundle`。
- Gradle 任务中：
  - `:app:assembleRelease` 生成 release APK。
  - `:app:bundleRelease` 生成 release AAB，不适合蒲公英上传。
  - `:app:assembleDebug` 可用于快速构建验证。
- 正式签名依赖本地 `kaochang.jks` 以及 `KAOCHANG_STORE_PASSWORD`、`KAOCHANG_KEY_ALIAS`、`KAOCHANG_KEY_PASSWORD`。
- 如果缺少正式签名文件或密码，必须明确说明生成的包可能不是正式签名包。

## 验证要求

- 能本地跑的验证优先自己跑。
- 代码改动至少跑相关单测、构建或静态检查之一。
- 涉及版本号或发包前，至少跑 `:app:assembleDebug`。
- 正式出包时跑 `:app:assembleRelease`。
- 跑不了验证时，必须说明原因、阻塞点和剩余风险。
- 汇报时先说结论和影响，再说验证情况。

## 日志、状态和编码

- 新增中文日志、状态文案、接口说明后，要自查 UTF-8、乱码、Unicode 转义混乱。
- 状态字段、状态值、开关值首次出现时，要写清中文含义。
- 日志要帮助现场排障，避免无意义刷屏。
- 修改日志口径时，要避免同一状态在不同地方出现不同中文解释。

## Git 提交

- 默认使用中文 commit message。
- 提交风格：

```text
类型(可选范围): 中文摘要

中文说明影响、验证、风险
```

- 常用类型包括 `fix`、`feat`、`chore`、`docs`、`refactor`。
- 提交前检查 diff，确认没有混入无关文件。

## 回复风格

- 默认中文回复。
- 结果汇报先说结论和影响，再说验证情况。
- 少讲空话，避免只列文件清单。
- 如果涉及状态、开关、版本、签名、产物类型，首次出现时要写清含义。
